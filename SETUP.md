# 비밀번호 잠금 + 기기 간 동기화 설정

카드 정보는 필요 없습니다. **Cloudflare 대시보드 + `git push`**만으로 끝납니다.

메뉴 이름은 Cloudflare가 자주 바꿉니다. 못 찾으면 그 화면을 캡처해서 물어보세요.

각 단계 끝에 **확인**이 있습니다. 확인이 안 되면 다음으로 넘어가지 마세요.

> **Access를 이미 만들어 두셨다면** Zero Trust → Access → Applications에서
> 그 application을 삭제하세요. 안 그러면 로그인을 두 번 하게 됩니다.

---

## 1단계 — 비밀번호 설정값 만들기

내려받은 폴더의 `tools/hash.html`을 **더블클릭해서 브라우저로 엽니다.**

계산은 그 브라우저 안에서만 일어납니다. 비밀번호는 어디로도 전송되지 않으니
인터넷을 끊고 하셔도 됩니다.

비밀번호를 두 번 입력하고 **만들기**를 누르면 값 세 개가 나옵니다.
계산에 몇 초 걸립니다. 세 값을 메모장에 복사해 두세요.

- `MEMO_SALT`
- `MEMO_VERIFIER`
- `MEMO_ITERATIONS`

**비밀번호는 12자 이상, 다른 곳에서 안 쓰는 것으로 정하세요.** 이 하나가
메모 전체를 지킵니다. 만들어진 값으로는 비밀번호를 되돌릴 수 없으니,
비밀번호 자체도 따로 기억해 두셔야 합니다.

> **확인** — 세 값이 화면에 나왔고 복사해 뒀습니다.

---

## 2단계 — 데이터베이스 만들기

대시보드에서 **Storage & Databases → D1**로 갑니다.

**Create database** → 이름은 `memo-db`.

만든 데이터베이스를 열고 **Console** 탭으로 갑니다.
저장소의 `schema.sql` 파일 내용을 통째로 복사해 붙여넣고 실행하세요.

> **확인** — Tables 탭에 `memos`, `sessions`, `login_attempts` 세 개가 보입니다.

---

## 3단계 — Pages에 연결하기

**Workers & Pages → memo → Settings**로 갑니다.

**바인딩(Bindings)** 에 D1을 추가합니다.

| 항목 | 값 |
| --- | --- |
| Variable name | `DB` |
| D1 database | `memo-db` |

이름은 반드시 대문자 `DB`여야 합니다. 코드가 이 이름으로 찾습니다.

**환경 변수(Environment variables)** 에 1단계 값 세 개를 넣습니다.

| 이름 | 값 | 종류 |
| --- | --- | --- |
| `MEMO_SALT` | 1단계의 salt | 일반 |
| `MEMO_VERIFIER` | 1단계의 verifier | **Secret (암호화)** |
| `MEMO_ITERATIONS` | `600000` | 일반 |

`MEMO_VERIFIER`는 **Encrypt** 또는 **Secret**을 선택해 저장하세요.
한 번 저장하면 다시 볼 수 없는 대신, 대시보드가 뚫려도 노출되지 않습니다.

> **중요** — 바인딩과 환경 변수는 **다시 배포해야 적용됩니다.** 4단계가 그 역할을 합니다.

---

## 4단계 — 코드 올리기

`functions`, `lib`, `tools` 폴더와 `_headers` 파일이 새로 생겼습니다.
폴더째 복사해 넣고 올리세요.

```powershell
git add .
git commit -m "비밀번호 잠금과 서버 저장 추가"
git push
```

Cloudflare가 자동으로 다시 배포합니다.

> **확인** — tisave.com에 접속하면 잠금 화면이 뜹니다. 비밀번호를 넣으면
> 메모장이 열립니다. 폰에서 같은 주소에 접속해 같은 비밀번호를 넣으면
> 같은 메모가 보입니다.

---

## 5단계 — 게시판 켜기 (선택)

게시판은 메모와 달리 **누구나 읽고 쓸 수 있는 공개 영역**입니다.
주소는 `tisave.com/board` 이고, 개인 메모와는 완전히 분리돼 있습니다.
게시판 코드는 `memos` 테이블을 건드리지 않습니다.

### 5-1. 표 네 개 만들기

D1 콘솔(`memo-db` → Console)에서 `schema-board.sql` 의 내용을
**한 문장씩** 실행하세요. `posts`, `comments`, `board_limits`, `files` 가 생깁니다.

> **확인** — `/tables` 를 실행하면 표가 일곱 개가 됩니다.
> (서버가 처음 돌면 설정용 `site_settings` 표를 스스로 만들어 여덟 개가 됩니다.)

나중에 생긴 것 — 글의 공개 범위 열(`visibility`)과 `site_settings` 표 — 은 **서버가
알아서 붙입니다.** 이미 게시판을 쓰고 있었어도 따로 실행할 SQL 은 없습니다.

### 5-2. 사람 확인(Turnstile) 키 만들기

Turnstile은 Cloudflare가 무료로 주는 봇 차단기입니다. **카드 등록이 필요 없습니다.**
사람은 대부분 클릭도 없이 지나가고 봇은 거의 다 걸립니다.

1. Cloudflare 왼쪽 메뉴 → **Turnstile** → **Add widget**
2. 이름은 아무거나 (예: `memo-board`)
3. **Hostname** 에 `tisave.com` 을 넣습니다. 임시 주소로도 시험할 거면
   `memo-2np.pages.dev` 도 같이 넣으세요.
4. Widget Mode 는 **Managed** 로 둡니다.
5. 만들면 키 두 개가 나옵니다 — **Site Key** 와 **Secret Key**

### 5-3. 환경 변수 두 개 추가

**Workers & Pages → memo → Settings → Variables and Secrets → Production**

| 이름 | 값 | 종류 |
| --- | --- | --- |
| `TURNSTILE_SITEKEY` | Site Key | 일반 (원래 브라우저에 노출되는 값입니다) |
| `TURNSTILE_SECRET` | Secret Key | **Secret / Encrypt** |

저장한 뒤 **재배포**해야 적용됩니다.

> **키를 안 넣어도 게시판은 돌아갑니다.** 그때는 사람 확인만 빠지고
> 나머지 방어(허니팟, 도배 제한, 길이 제한, Origin 검사)는 그대로 작동합니다.
> 키를 넣는 순간 화면과 서버 양쪽이 자동으로 사람 확인을 켭니다.

### 5-4. 게시판 쓰는 법

- **글쓰기** — 닉네임(비우면 익명)과 **글 비밀번호**를 정합니다.
  그 비밀번호로 나중에 본인 글을 고치거나 지웁니다.
  **사이트 비밀번호를 여기에 쓰지 마세요.** 용도가 다릅니다.
- **주인 권한** — 메모에 로그인한 상태로 게시판에 가면 비밀번호 없이
  **아무 글이나 지울 수 있습니다.** 청소는 이걸로 하세요.
- **주인이 글 쓸 때** — 닉네임과 비밀번호 칸은 로그인 상태에서도 그대로 보입니다.
  - **비우고 올리면** `주인` 이름으로 올라갑니다. 비밀번호가 없고, 로그인해 있는 한
    언제든 지울 수 있습니다.
  - **채워서 올리면** 보통 글로 올라갑니다. 그 비밀번호로 로그아웃 상태에서도
    고치고 지울 수 있습니다. (물론 주인 자격으로도 여전히 지울 수 있습니다)
- **도배 제한** — 한 IP에서 글은 30초에 하나·하루 20개, 댓글은 10초에 하나·하루
  100개까지입니다. 막힌 뒤에도 계속 두드리면 최대 60분까지 잠깁니다.
  주인은 이 제한을 받지 않습니다.

### 5-5. HTML 파일 올리고 보기

**올리는 건 주인만, 보는 건 누구나** 할 수 있습니다.

1. 메모장(`/`)에서 사이트 비밀번호로 로그인한 뒤 게시판으로 옵니다.
2. **글쓰기**를 누르면 내용 칸 아래에 **HTML 고르기** 가 보입니다 (로그인 상태에서만).
   `.html` 파일을 고르고 **올리기**를 누르면 글과 함께 올라갑니다.
   여러 개를 한 번에 골라도 되고, 한 파일 **500KB**, 글마다 **10개**까지입니다.
3. 이미 올린 글에 더 붙이려면 그 글을 열고 아래의 **HTML 올리기** 를 누릅니다.
4. 첨부 줄의 **보기**를 누르면 글 안에서 바로 실행되고, **새 창**을 누르면
   크게 볼 수 있습니다. 로그아웃 상태의 방문자도 똑같이 볼 수 있습니다.

**왜 주인만 올릴 수 있나 —** 여기 올라간 HTML은 `tisave.com` 주소로 돌아다닙니다.
아무나 올릴 수 있으면 남의 피싱 페이지가 내 도메인 이름을 달게 됩니다.

**어떻게 안전한가 —** 올린 HTML은 격리된 출처(opaque origin)로 내려옵니다.
안에서 자바스크립트와 CSS는 정상적으로 동작하지만, `tisave.com`의 쿠키·저장소·
로그인된 API에는 닿지 못합니다. **그래서 첨부 페이지가 내 메모를 읽어갈 수 없습니다.**
이 격리는 `functions/api/board/files/[id]/raw.js`의 응답 헤더 한 줄이 담당합니다.
그 파일의 `sandbox` 설정을 손대지 마세요. 특히 `allow-same-origin`을 넣으면
격리가 통째로 풀립니다.

### 5-6. 잠긴 글과 비공개 글

글쓰기(와 글 수정) 화면의 **공개 범위**에서 고릅니다.

| 공개 범위 | 목록에서 | 글을 열면 | 볼 수 있는 사람 |
| --- | --- | --- | --- |
| **공개** | 제목 그대로 | 내용·댓글·첨부 | 누구나 |
| **잠금** | "잠긴 글" + `잠금` 표시 | **열기** 버튼만 | 글 비밀번호를 아는 사람, 주인 |
| **비공개** | 주인에게만 보임 | 주인만 | 주인(로그인) |

**저작권물을 혼자 보려고 올릴 때**

- 로그인한 기기에서만 볼 거면 → **비공개**. 가장 확실합니다.
- 로그인하지 않은 기기(회사 PC 등)에서도 볼 거면 → 글 비밀번호를 정하고 **잠금**.
  그 기기에서 글을 열고 **열기**를 눌러 글 비밀번호를 넣습니다. **2시간** 동안 열려 있고,
  지나면 다시 넣으면 됩니다.
- HTML 첨부도 글과 같은 규칙입니다. 첨부 주소를 알아도 잠긴 글·비공개 글의 첨부는
  열리지 않습니다.

**규칙**

- **잠금**에는 글 비밀번호가 꼭 있어야 합니다. 주인이 비밀번호 없이 쓴 글이면
  **비공개**를 고르세요.
- **비공개**는 주인만 고를 수 있습니다. 손님 화면에는 그 선택지가 없습니다.
- 이미 올린 글도 **수정**에서 공개 범위를 바꿀 수 있습니다.
- 글 비밀번호를 **5번 틀리면** 잠깐 막힙니다(1분부터 두 배씩, 최대 64분).

> **알아둘 것** — 잠금·비공개는 *이 사이트에서* 남에게 안 보이게 하는 장치입니다.
> 데이터베이스(D1)에는 암호화 없이 저장되므로 Cloudflare 계정 자체는 따로 잘 지키세요.

---

## 6단계 — 사이트 비밀번호 바꾸기 (필요할 때)

1. `tisave.com` 에 로그인합니다.
2. 위쪽 **비밀번호 바꾸기**를 누릅니다.
3. 지금 비밀번호, 새 비밀번호(12자 이상)를 두 번 넣고 **바꾸기**를 누릅니다.
   계산에 몇 초 걸립니다.
4. "바꿨습니다"가 뜨면 끝입니다.

> **확인** — 폰 등 다른 기기는 로그아웃돼 있고, 새 비밀번호로만 들어가집니다.

- 이때도 **비밀번호 원문은 서버로 가지 않습니다.** 계산한 값만 갑니다.
- 새 값은 D1 의 `site_settings` 표에 저장되고, 환경 변수(`MEMO_SALT`/`MEMO_VERIFIER`)보다
  우선합니다. **환경 변수는 지우지 말고 그대로 두세요.** 게시판 IP 해시에도 씁니다.
- 예전에 환경 변수 값을 채팅 등에 붙여 넣은 적이 있어 걱정되면, 이 기능으로 한 번
  바꾸세요. 그 뒤로 그 값은 로그인에 쓰이지 않습니다.

**새 비밀번호를 잊었을 때** — D1 콘솔에서 아래를 실행하면 **1단계에서 정한(환경
변수) 비밀번호**로 돌아갑니다. 들어간 뒤 다시 바꾸면 됩니다.

```sql
DELETE FROM site_settings WHERE key = 'auth';
```

---

## 안 될 때

**게시판에서 "사람 확인을 먼저 마쳐 주세요"가 계속 뜸**
Turnstile 위젯이 안 보이거나 확인이 안 끝난 상태입니다.
F12 콘솔에 CSP 오류가 있으면 `_headers` 에 `challenges.cloudflare.com` 이
들어 있는지 보세요. 위젯은 보이는데 거절당하면 Turnstile 위젯의
**Hostname** 에 지금 접속한 주소가 들어 있는지 확인하세요.

**게시판에서 "조금 천천히 써 주세요"**
도배 제한입니다. 안내된 시간만큼 기다리면 풀립니다. 시험 중이라 답답하면
D1 콘솔에서 `DELETE FROM board_limits;` 를 실행하세요.

**게시판 글이 안 보이고 오류만 뜸**
5-1을 안 했을 가능성이 큽니다. D1 콘솔에서 `/tables` 로
`posts`, `comments`, `board_limits`, `files` 가 있는지 확인하세요.

**HTML 올리기 버튼이 안 보임**
로그인 상태에서만 보입니다. 메모장(`/`)에서 먼저 사이트 비밀번호를 넣고
오세요. 그다음 **글쓰기** 화면(새 글)이나 **글을 하나 연 화면**에 나타납니다.
글 목록 화면과 글 **수정** 화면에는 없습니다.

**첨부한 페이지가 빈 화면으로 뜸**
첨부는 격리된 출처에서 실행됩니다. 그래서 `localStorage`, `sessionStorage`,
쿠키에 손을 대면 그 자리에서 오류가 나고 스크립트가 멈춥니다. F12 콘솔에
`SecurityError` 가 보이면 이 경우입니다. 저장 기능을 빼거나 변수로만 굴리게
고쳐서 다시 올리세요. **이 격리는 개인 메모를 지키는 장치라 풀면 안 됩니다.**
(바깥에서 스크립트나 글꼴을 불러오는 것 자체는 막지 않습니다.)

**잠긴 글이 방금까지 보이다가 다시 잠김 / 첨부가 "없는 파일입니다"로 뜸**
잠긴 글은 한 번 열면 2시간 동안 열려 있습니다. 지났으면 **열기**를 다시 누르세요.
주인이라면 로그인이 풀렸는지(메모장에서 잠금 화면이 뜨는지) 확인하세요.

**잠긴 글을 열 때 "시도가 너무 많습니다"**
글 비밀번호를 5번 틀려서 막힌 상태입니다. 안내된 시간만큼 기다리거나, 주인이면
로그인해서 보세요. 급하면 D1 콘솔에서 `DELETE FROM login_attempts;` 로 풉니다.

**공개 범위에서 "잠긴 글은 글 비밀번호로 엽니다"**
잠금에는 글 비밀번호가 필요합니다. 비밀번호 칸을 채우거나, 주인만 볼 거면
**비공개**를 고르세요.

**비밀번호 바꾸기에서 "지금 비밀번호가 맞지 않습니다"**
지금 로그인에 쓰는 비밀번호를 넣어야 합니다. 여기서 틀린 것도 로그인과 같이 세어
5번이면 잠깁니다.

**"데이터베이스를 준비하지 못했습니다"**
서버가 새 표·열을 붙이다 실패한 경우입니다. 잠시 뒤 새로고침하세요. 계속되면
2단계의 `schema.sql` 을 D1 콘솔에서 실행했는지 확인하세요.

**"MEMO_SALT와 MEMO_VERIFIER 환경 변수가 설정되지 않았습니다"**
3단계를 안 했거나, 넣고 나서 재배포를 안 했습니다.
Deployments에서 **Retry deployment**를 누르세요.

**"D1 데이터베이스가 연결되지 않았습니다"**
바인딩 이름이 `DB`가 맞는지 확인하고 재배포하세요.

**맞는 비밀번호인데 계속 틀렸다고 함**
`MEMO_SALT`나 `MEMO_VERIFIER`를 복사할 때 앞뒤 공백이나 줄바꿈이 섞였을
가능성이 큽니다. 1단계를 다시 해서 새 값으로 바꾸세요.

**"시도가 너무 많습니다"**
5번 틀리면 잠깁니다. 안내된 시간만큼 기다리거나, D1 콘솔에서
`DELETE FROM login_attempts;` 를 실행하면 바로 풀립니다.

**잠금 화면에서 멈춤**
F12 → Network 탭을 열고 새로고침하세요. `/api/login` 요청의 상태 코드를
보면 위 중 어느 경우인지 알 수 있습니다.

---

## 이 잠금이 실제로 막는 것

**비밀번호가 서버로 가지 않습니다.** 브라우저가 PBKDF2를 60만 번 돌려 만든
값만 전송합니다. 서버도, 데이터베이스도, 로그도 비밀번호를 모릅니다.

**환경 변수가 유출돼도 비밀번호를 알 수 없습니다.** 저장된 건 검증값뿐이고
거기서 되돌리려면 PBKDF2를 60만 번씩 돌려가며 전수 조사해야 합니다.

**세션 쿠키를 자바스크립트가 못 읽습니다(HttpOnly).** 사이트에 악성 스크립트가
끼어들어도 로그인 상태를 훔쳐갈 수 없습니다.

**다른 사이트에서 온 요청은 거부합니다(SameSite + Origin 검사).** 다른 탭에서
열린 악성 페이지가 몰래 메모를 지우게 할 수 없습니다.

**데이터베이스에는 세션 토큰의 해시만 있습니다.** D1을 통째로 봐도 남의
로그인 상태를 흉내 낼 수 없습니다.

**비밀번호 5회 실패 시 IP별로 잠깁니다.** 실패할수록 잠금 시간이 두 배씩
늘어 최대 64분까지 갑니다. 사이트 비밀번호 바꾸기, 게시판 글 비밀번호(잠긴 글
열기 포함)도 같은 방식으로 잠깁니다.

**잠긴 글·비공개 글은 주소를 알아도 안 열립니다.** 글 상세, 댓글, 첨부 모두
서버가 매번 볼 자격(주인 세션 또는 2시간짜리 열람 토큰)을 확인합니다. 토큰은
서버만 아는 비밀로 서명돼 있어 위조하거나 다른 글에 쓸 수 없습니다.

**비밀번호를 바꾸면 다른 기기는 모두 끊깁니다.** 누가 비밀번호를 알았을까 봐
바꾸는 경우, 그 사람의 로그인도 그 자리에서 끝납니다.

**보안 헤더가 붙습니다(`_headers`).** 외부 스크립트 실행, 다른 사이트에
끼워 넣기(iframe), HTTP 접속을 전부 막습니다.

---

## 알아둘 것

**비밀번호를 아는 사람은 같은 메모장을 봅니다.** 사람별로 나뉘지 않습니다.
혼자 쓰거나 믿는 사람끼리 쓰는 용도입니다. 각자 다른 메모장이 필요하면
계정 기능을 따로 만들어야 합니다.

**메모는 암호화 없이 저장됩니다.** 계정 비밀번호, 카드번호, 주민번호처럼
유출되면 곧바로 피해가 되는 정보는 넣지 마세요.

**`tools/hash.html`과 `lib/auth.js`는 사이트에서 열립니다.** 둘 다 비밀이
없는 계산기와 유틸이라 문제없습니다. 신경 쓰이면 설정을 마친 뒤
`tools` 폴더를 지우고 다시 push해도 됩니다.

**무료 한도는 걱정할 필요 없습니다.** D1은 데이터베이스 하나당 500MB,
하루 쓰기 10만 건입니다. 메모 한 건이 1KB 정도라 수십만 개를 써도 남습니다.

**비밀번호를 바꾸려면** 6단계처럼 사이트에서 바로 바꾸면 됩니다. 메모는 그대로
남고, 다른 기기의 로그인은 자동으로 끊깁니다. (예전처럼 1단계를 다시 해서 환경
변수를 교체하는 방법은, 사이트에서 한 번이라도 바꾼 뒤로는 효과가 없습니다.
사이트에서 바꾼 값이 우선하기 때문입니다.)
